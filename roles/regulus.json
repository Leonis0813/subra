{
  "name": "regulus",
  "run_list": [
    "recipe[mysql::install]",
    "recipe[rvm::install]",
    "recipe[nginx]",
    "recipe[redis]",
    "recipe[rstudio]",
    "recipe[regulus::ruby]",
    "recipe[regulus]"
  ]
}
