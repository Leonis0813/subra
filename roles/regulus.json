{
  "name": "regulus",
  "run_list": [
    "recipe[mysql::install]",
    "recipe[rvm::install]",
    "recipe[nginx]",
    "recipe[nginx::conf]",
    "recipe[regulus::ruby]",
    "recipe[regulus::deploy]"
  ]
}
