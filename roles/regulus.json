{
  "name": "regulus",
  "run_list": [
    "recipe[mysql::install]",
    "recipe[rvm::install]",
    "recipe[regulus::ruby]",
    "recipe[regulus::deploy]",
    "recipe[regulus::mysql]",
    "recipe[regulus::user]",
    "recipe[regulus::samba]",
    "recipe[regulus::cron]"
  ]
}
