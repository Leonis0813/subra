{
  "name": "regulus",
  "run_list": [
    "recipe[mysql::install]",
    "recipe[rvm::install]",
    "recipe[nginx::user]",
    "recipe[nginx::install]",
    "recipe[regulus::ruby]",
    "recipe[regulus::deploy]"
  ]
}
