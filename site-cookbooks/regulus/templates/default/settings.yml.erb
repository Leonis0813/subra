mysql:
  host: localhost
  port: 3306
  user: root
  password: <%= Chef::EncryptedDataBagItem.load('regulus', 'mysql')['password'] %>
  database: <%= node[:zosma][:app_name] %>

prediction:
  wait_latest: <%= node[:regulus][:wait_latest] %>
