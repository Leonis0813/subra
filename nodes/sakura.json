{
  "name": "sakura",
  "environment": "production",
  "run_list": [
    "recipe[git::install]",
    "recipe[jenkins::install]",
    "recipe[jenkins::user]",
    "recipe[jenkins::plugin]",
    "recipe[jenkins::job]",
    "recipe[sphinx::install]",
    "recipe[nfs::install]",
    "recipe[nfs::iptables]",
    "recipe[alterf::nfs]",
    "role[algieba]",
    "role[alterf]",
    "role[regulus]"
  ]
}
