{
  "name": "centos7-ana",
  "environment": "compute",
  "run_list": [
    "recipe[nfs::install]",
    "recipe[nfs::mount]",
    "recipe[R::install]",
    "recipe[alterf::rc.local]",
    "recipe[alterf::deploy]",
    "recipe[regulus::deploy]"
  ]
}
