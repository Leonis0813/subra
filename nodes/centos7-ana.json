{
  "name": "centos7-ana",
  "environment": "compute",
  "run_list": [
    "recipe[nfs::install]",
    "recipe[nfs::mount]",
    "recipe[R::install]",
    "recipe[alterf::deploy]",
    "recipe[alterf::rc.local]",
    "recipe[alterf::nfs]",
    "recipe[regulus::deploy]"
  ]
}
